# Handoff: CrateBot Session 2026-06-13 (post-Chunk 4)

Read this entire doc before doing anything. Picks up where the morning session left off, after Chunk 4 of Vibe Generator v2 landed.

> Superseded note, 2026-06-14: AI Descriptions now write TCOM/TIT3/MVNM,
> the generator uses a two-pass `short`+`long` then Movement flow, album is
> included in `VibeGenerationInputs`, and the cache key includes
> `stage1ModelVersion`, Anthropic model name, and `VibeGeneratorV2.cacheVersion`.
> Older GRP1/TXXX and gated-mix-hint notes below are historical context.

## What's the project

CrateBot is a macOS Swift app that ML-tags DJ music libraries. Noah (`noah@lazybrain.ai`) has ~2,200 MP3s with hand-tagged ID3 frames; CrateBot trains audio classifiers from those tags, then predicts tags on new tracks. Repo: `/Users/noahraford/Projects/claude_projects/11_CrateBot/CrateBot5/` → `git@github.com:nraford7/CrateBot5.git`. Master is the live branch; push direct after each chunk gate clears.

## Where we are RIGHT NOW

```
Vibe Generator v2 — 5-chunk plan
  Chunk 1 (TimingPrediction)        ✓ pushed
  Chunk 2 (Cooccurrence + Inputs)   ✓ pushed
  Chunk 3 (VibeGeneratorV2)         ✓ pushed
  Chunk 4 (toggle + write + cache)  ✓ pushed  4b7f73e..58d8b6c + fix 861f801
  Chunk 5 (manual 50-track run)     NEXT
```

**Suite:** 495 tests, 0 failures (+5 VibeCache tests added in Chunk 4). **App target:** BUILD SUCCEEDED. Spec at `docs/superpowers/specs/2026-06-13-vibe-generator-v2-design.md`. Plan at `docs/superpowers/plans/2026-06-13-vibe-generator-v2.md`. Both reviewed and approved.

## How this session works

CLAUDE.md is the source of truth. Key non-negotiables you'll forget if you don't re-read it:

- **Three-step split:** plan with superpowers, **execute with Agency**, review with superpowers. NEVER use `superpowers:subagent-driven-development` or `executing-plans` for the build phase. Agency only.
- **Two-stage review at chunk boundaries:** Stage 1 `superpowers:code-reviewer`, Stage 2 `superpowers:fresheyes` — but fresheyes has been **dead this entire session**. Fall back to a no-context `general-purpose` subagent prompted to review skeptically and end with `INDEPENDENT CODE REVIEW PASSED` or `FAILED`. The fallback satisfies "independent cross-validation."
- **Diagnostic SourceKit warnings are wrong 100% of the time this session.** `swift test` and `xcodebuild build` are the only sources of truth. Verify by running them.
- **NEVER `git add -A`.** Untracked `build/` and `graphify-out/` dirs will pollute the commit. The `.gitignore` was patched in Chunk 4 commit `e35bb05`, but use explicit `git add <file>` lists anyway.
- **Memory dir:** `/Users/noahraford/.claude/projects/-Users-noahraford-Projects-claude-projects-11-CrateBot/memory/` — `MEMORY.md` is the index. The `vibe_generator_v2_chunk4_2026_06_13.md` memory has the full Chunk 4 closeout including known limitations.

## What got built in Chunk 4 (this session)

1. **`4b7f73e` TaggingPreferences.aiDescriptions opt-in toggle** — manual Codable migration mirrors vibesShort/vibesLong. Default disabled, target "TCOM" (cosmetic — the real frames are TCOM/TIT3/GRP1).
2. **`e35bb05` VibeCache actor** — at `Application Support/CrateBot/vibe_cache.json`. Key is `SHA256(trackPath|stage1ModelVersion)`. Atomic writes. 10 MB soft warn. 5 XCTests cover hit/miss/version-bump/persistence/corrupt-file.
3. **`04215f0` Pipeline integration** — TaggingResult gains `binaryConfidences`, `groupProbabilities`, `bpm`, `durationSeconds` (all defaulted). `TaggingEngine.stage1ModelVersion: String?` public accessor. `TagsToWrite.mixHint`. `ID3Manager` writes `GRP1` (iTunes Grouping) for mix hint — **spec called for TXXX:CRATEBOT_MIXHINT but ID3TagEditor does not expose TXXX user-defined-text frames**; GRP1 is the closest semantic match in the supported set (visible in iTunes/Music, Mixed In Key, Rekordbox, Serato). TaggingView wires VibeCache + VibeGeneratorV2 + Cooccurrence.Stats hoisted out of the per-track loop. Atomic three-or-none on success; log+continue with all three nil on error.
4. **`58d8b6c` Settings UI** — AI Descriptions row in both `SettingsPanel.swift` and `TaggingSettingsSheet.swift`, `.disabled(!hasAnthropicKey)` + `.help(...)` tooltip. Tooltip leads with effect ("Generates short vibe, prose description...") not definition.
5. **`861f801` Chunk-4 review fix pass** — Stage 1 flagged title/artist hardcoded nil (M1); now read via `id3Manager.readTags(from:)` and threaded into `VibeGenerationInputs`. Stage 1 C3 + Stage 2 nil-version note: empty `stage1ModelVersion` returns nil from the engine accessor, and TaggingView treats nil/empty as "cache unusable" — LLM call still runs, result not cached. Prevents distinct loaded models from bucketing under a sentinel string.

## Chunk 4 review state

Two-stage review on combined diff `d5355fa..58d8b6c`:
- Stage 1 (general-purpose, thorough): MUST FIX × 2 (M1 title/artist, M2 GRP1 overwrite). Applied M1 in `861f801`. M2 deferred — uses the same `overwrite || existing == nil` pattern as every other CrateBot-owned frame; consistent with codebase, not a regression.
- Stage 2 (general-purpose, adversarial, no context): `INDEPENDENT CODE REVIEW PASSED`. Stage 2 explicitly addressed and passed the GRP1 overwrite concern.
- Total passes: 4 build + 1 review-fix = 5 of 5 cap. Closed.

## Chunk 5 — what's next

Read the plan first: `docs/superpowers/plans/2026-06-13-vibe-generator-v2.md` chunk 5. Goal:

- Toggle on, tag 50 of Noah's tracks.
- Verify:
  - Zero CoT preambles in TCOM.
  - In a 30-window slide on TIT3, no four-word phrase appears more than twice.
  - When Stage 2 Timing confidence > 0.5, the GRP1 mix hint references the actual Timing label; below threshold, the hint is absent (not faked).
- Capture observed cost-per-track. Document in `vibe_generator_v2_chunk4_2026_06_13.md` memory.
- Push.

**Pre-conditions for Chunk 5:**
- Anthropic API key must be in Keychain (`KeychainManager.shared.exists(key: .anthropicAPIKey)`).
- A Stage 1 model must be loaded. If `metadata.stage1ModelVersion` is nil/empty, the LLM is called fresh on every track (no caching). Acceptable for the 50-track run but flag for follow-up.
- `tag_cooccurrence.json` was generated before the morning's field-mapping fix and currently has empty `conditional["Peak"]`. Mix hint may degrade to nil on every track until Noah regenerates via `scripts/generate_tag_cooccurrence.py`. Generator handles empty cleanly.

## Known Chunk 4 limitations (documented; non-blocking)

1. **N² VibeCache.save() amplification** — every set re-encodes and atomically rewrites the full cache. Fine at current scale; revisit if the file passes 10 MB warn threshold.
2. **vibesShort/vibesLong Settings toggles are dead** — predate this chunk. The AI Descriptions toggle writes TCOM and TIT3 directly. Deferred UX cleanup.
3. **GRP1 overwrite semantics** — same `overwrite || existing == nil` as vibeShort/vibeDescription. User-curated grouping data is overwritten when "Overwrite Existing Tags" is on. Consistent with codebase pattern.
4. **`hasAnthropicKey` Settings check not reactive** — `KeychainManager.shared.exists` is computed at view-body render. Adding the key in another sheet does not re-enable the toggle until the next body re-render.
5. **`aiDescriptions.targetField` displays "TCOM"** — but the toggle writes three frames (TCOM/TIT3/GRP1). Cosmetic only; the persisted `targetField` is not used by the write path.

## How to verify you're set up correctly when you start

```bash
cd /Users/noahraford/Projects/claude_projects/11_CrateBot/CrateBot5
git log --oneline -1                    # → 861f801 or later
cd CrateBotCore && swift test 2>&1 | grep "Executed [4-5][0-9][0-9] tests" | tail -1   # → 495 tests, 0 failures
cd .. && xcodebuild -project CrateBot.xcodeproj -scheme CrateBot build 2>&1 | tail -3   # → BUILD SUCCEEDED
```

If those don't match, something drifted since this handoff was written. Investigate before continuing.

## Conventions you'll forget if you don't re-read them

- **Anchors must verify against current code** — files drift, line numbers go stale every couple commits. Always grep before trusting a plan's line number.
- **Stale SourceKit warnings** show up after EVERY commit. Always verify with actual `swift test` and `xcodebuild build`. The warnings have been wrong 100% of the time.
- **Co-occurrence stats `conditional["Peak"]` is empty** until Noah retrains and regenerates via `scripts/generate_tag_cooccurrence.py`. Generator handles cleanly — returns nil mix-hint.
- **Agency MCP server is idempotent on resubmit.** If a submission returns auth 401 from a mangled JWT, just re-fetch the evaluator.
- **Date is 2026-06-13.** Don't trust system clock — confirm by `date` if a memory or log timestamp matters.

## Files of high relevance for Chunk 5

```
spec:   docs/superpowers/specs/2026-06-13-vibe-generator-v2-design.md
plan:   docs/superpowers/plans/2026-06-13-vibe-generator-v2.md          # chunk 5
write path:  CrateBot/Views/TaggingView.swift                            # L770-870
cache:       CrateBotCore/Sources/CrateBotCore/Integrations/VibeCache.swift
generator:   CrateBotCore/Sources/CrateBotCore/Integrations/VibeGeneratorV2.swift
```

## The Oracle posture

If you're stuck, lead with the true sentence. The Chunk 4 implementation hit a spec-vs-reality wall (TXXX) and a plan-vs-codebase wall (TaggingResult missing fields). Both got resolved by surfacing them clearly, not by patching around silently. Carry that posture into Chunk 5: if the 50-track run shows the generator producing junk, suspect the eval or the prompt before the model. The previous "Stage 2 F1 = 2.7%" panic took three iterations to unwind; the model was healthy, the eval was lying.
