# Audio Processing
librosa==0.10.1
soundfile==0.12.1
pydub==0.25.1

# ID3 Tag Management
mutagen==1.47.0

# Machine Learning
scikit-learn>=1.5.0
numpy==1.26.3
pandas==2.1.4
joblib==1.3.2

# Audio Fingerprinting
pyacoustid==1.3.0

# CLI Interface
click==8.1.7
rich==13.7.0

# Progress bars and logging
tqdm==4.66.1

# Testing
pytest==7.4.4
pytest-cov==4.1.0

# Retry logic for API calls
tenacity==8.2.3

# Note: pygame removed - audio playback handled by Electron's HTML5 audio
# The Python audio_player.py is not used by the server

# High-level audio features (Essentia pre-trained models)
# Optional - install for mood, danceability, vocals detection
# essentia-tensorflow>=2.1b6.dev1177  # Commented out - install separately if needed

# CLAP - semantic audio embeddings
# Optional - install for enhanced audio understanding
laion-clap>=1.1.4

# LLM-powered vibe generation
# Optional - install for Claude API integration
anthropic>=0.25.0

# Vocal transcription for hook detection
# Optional - install for local Whisper transcription
faster-whisper>=1.0.0

# API Server (for Electron frontend)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
websockets==12.0

# PyInstaller for packaging
pyinstaller==6.3.0

# Required by librosa/scikit for pkg_resources at runtime
setuptools==70.0.0
